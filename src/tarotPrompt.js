/**
 * 生成规则模块（公开仓库脱敏版）
 *
 * 本文件在公开仓库中不包含实际生产提示词。原始模块是经过约 50 个版本迭代
 * 打磨的完整系统提示词，包含逐条输出约束、模块结构权重、内部结构标记、
 * 反幻觉规则和第三方牌义参考资料，属于内部方法资产，不随公开仓库发布。
 *
 * 保留导出名与调用契约，使仓库在不含私密内容的情况下仍可安装、构建和审阅
 * 架构。若需运行完整生成能力，请在部署环境中以同名导出提供自有提示词。
 *
 * 原模块覆盖的规则域（仅列范畴，不展开具体指令与参数）：
 *   - 输出结构骨架：十个内容模块的顺序、职责与相对结构约束
 *   - 信息密度与篇幅：比例优先于字数的长度控制策略
 *   - 牌面解读层次：画面细节、正逆位、牌间关系、针对性倾向判断
 *   - 分析维度：动态/静态、灵数、四元素等内部分析层，融入叙事而不外报标签
 *   - 语境与状态：语境识别、客户状态判断、情绪语气与意象落地
 *   - 反幻觉约束：禁止剧本式脑补、禁止虚构因果归因、事实颗粒度镜像
 *   - 边界与合规：中立边界、免责触发分流、禁说教与高位姿态
 *   - 格式合规：禁用装饰符号与报幕式标签头、口语化校验
 *   - 缺省处理：问题为空、日常事件类问题的专项处理路径
 *
 * 详见公开作品集材料：docs/portfolio/
 */

export const TAROT_SYSTEM_PROMPT = `[生成规则模块已脱敏]

本公开仓库不包含实际生产提示词。请在部署环境中以 TAROT_SYSTEM_PROMPT
提供自有系统提示词，或参考 docs/portfolio/ 中的公开设计说明自行实现。`;
